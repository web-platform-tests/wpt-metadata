on:
  push:
    branches:
      - master
  pull_request: {}
name: test
jobs:
  test:
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v1
    - uses: actions/setup-go@v1
      with:
        go-version: 1.11.x
    - run: go build ./...
    - run: go test -v ./...
