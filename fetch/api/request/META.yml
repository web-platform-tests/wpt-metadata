links:
    - product: deno
      url: https://github.com/denoland/deno/issues/10993
      results:
        - test: request-init-stream.any.html
          subtest: Constructing a Request with a Request on which body.getReader().read() is called
        - test: request-init-stream.any.html
          subtest: Constructing a Request with a Request on which body.getReader() is called
        - test: request-init-stream.any.html
          subtest: Constructing a Request with a Request on which read() and releaseLock() are called
